import { ScrollArea } from "../ui/scroll-area";

export default function MessagingView() {
  return (
    <ScrollArea className="flex flex-col items-center flex-1 h-full overflow-x-hidden overflow-y-auto"></ScrollArea>
  );
}
